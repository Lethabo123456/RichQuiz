Using quantifiers, we see that the relation R on the set A is symmetric if  
  
  ∀𝑎∀𝑏((𝑎, 𝑏) ∈ 𝑅 → (𝑏, 𝑎) ∈ 𝑅).  

  Similarly, the relation R on the set A is antisymmetric if  
∀𝑎∀𝑏(((𝑎, 𝑏) ∈ 𝑅 ∧ (𝑏, 𝑎) ∈ 𝑅) → (𝑎 =  𝑏)). 
Definition 51 
A relation R on a set A is called transitive if whenever (a, b) ∈ R and (b, c) ∈ 
R, then (a, c) ∈ R, for all a, b, c ∈ A. 
Using quantifiers we see that the relation R on a set A is transitive if we have 
∀a∀b∀c(((a, b) ∈ R ∧ (b, c) ∈ R) → (a, c) ∈ R). 
3.6.5 Combining Relations 
Because relations from A to B are subsets of A × B, two relations from A to B 
can be combined in any way two sets can be combined. 
Let A = {1, 2, 3} and B = {1, 2, 3, 4}. The relations R1 = {(1, 1), (2, 2), (3, 3)} and 
R2 = {(1, 1), (1, 2), (1, 3), (1, 4)} can be combined to obtain 
 
 
Definition 52 
Let R be a relation from a set A to a set B and S a relation from B to a set C. 
The composite of R and S is the relation consisting of ordered pairs (a, c), 
where a ∈ A, c ∈ C, and for which there exists an element b ∈ B such that  
(a, b) ∈ R and (b, c) ∈ S. We denote the composite of R and S by S∘R. 
