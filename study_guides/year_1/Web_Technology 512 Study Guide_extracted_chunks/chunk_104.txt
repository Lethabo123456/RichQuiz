2], etc, 
we have replaced the index number with the variable name counter. We can do this 
because counter changes each time round the loop. Javascript knows what is in the 
counter variable and uses this as the index. After the equal sign, we are assigning 
whatever is in the counter variable (plus 1) to each position in the array. (Can you see 
why we would need to add 1?) 
 
4.2 JAVASCRIPT FUNCTIONS 

 
66 
 
FOUR | ARRAYS AND FUNCTIONS 
 
The scripts we have at the moment are set to run 
as soon as the page loads in the browser. But they 
do not have to. A better way is to put the code 
inside something called a function. A function is 
just a separate code segment that you call into 
action. In fact, they do not do anything unless you 
do call them into action. Let's see how they work. 
 
 
Function syntax: 
function myFunction()  
{ 
 //code to be executed comes here. 
} 
You start with the word function (lowercase "f"). After a space you type a name for 
your function. Function