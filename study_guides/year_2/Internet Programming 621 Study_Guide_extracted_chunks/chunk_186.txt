te folder, and then open a 
Web browser and type the following into its address bar:  
http://localhost/testing.html  
  
Now press Enter. If all has gone according to plan, you should see something like Figure 4.  
  
 
Figure 4  
  

92  
If your Apache server is not running on the standard HTTP port (80) — for example, if you installed 
MAMP and used its default port of 8888 — modify your URL appropriately; for example: http:// 
localhost:8888/testing.htm l.  
  
This means that Apache is up and running and you have successfully located the Web server’s 
document root. If you do not get this page, it is likely that one of two things is happening: If you get 
a 404 Not Found error, this means that the testing.html file is not in the Web Server’s document 
root. Double - check the location of the document root folder — take a look at the documentation if 
necessary — and make sure your testing.html file is inside the folder. If you get a Connection 
Refused error, the Apache Web serve