e there are three distinct 
groupings of data; each one of those groups is a cluster. The main aim is to find structure 
within a given set of data. Because there are a lot of algorithms to choose from, clustering 
casts a wide net. This is where experimentation comes in handy; which algorithm is the right 
choice? Sometimes you just need to put some code together and play with it.  
 
 
 
                                                                                             Figure 4.2: Clustering 
 
4.3.1 Why using Clustering?  
Clustering is a widely used ML approach. Although it might seem simple, do not 
underestimate the importance of grouping multivariate data into refined groupings. See the 
applicability of clustering in real life in Table 4.2. 

P a g e  | 43 
 
 
4.3.2 Clustering Models 
Graph models can show cluster-like properties when the nodes start showing as small subsets 
connected with one main edge (see Figure 4.3). You can also approach simple clustering with