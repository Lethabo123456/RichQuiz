ot foolproof, help safeguard a wireless network: 
 
■ Connect to the router and change the default logon (admin) and password (password) for the 
router. These defaults are widely known by hackers. 
 
 
180 

■ Create a service set identifier (SSID). This is a 32-character unique identifier attached to the 
header portion of packets sent over a wireless network that differentiates one net- work from 
another. All access points and devices attempting to connect to the network must use the same 
SSID. 
■ Configure the security to WPA. Surprisingly, many routers are shipped with encryption 
turned off. 
■ Disable SSID broadcasting. By default, wireless routers broadcast a message 
communicating the SSID so wireless devices within range (such as a laptop) can identify and 
connect to the wireless network. If a device doesn’t know the wireless network’s SSID, it 
cannot connect. Disabling the broadcasting of the SSID will discourage all but the most 
determined and knowledgeable hackers. 
■